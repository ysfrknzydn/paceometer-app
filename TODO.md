# TODO

## MVP funnel status (`research_plan.qmd` `@sec-mvp-funnel`)

| Stage | Status |
|---|---|
| Core Function | Done (2026-07-15) — zone indicator (`marginalSecondsSaved`, ±10mph/60s threshold) |
| Core Loop | Done (2026-07-15) — hysteresis (`ZONE_HYSTERESIS_SECONDS`) + flash cue on state change |
| Accessory Features | Done (2026-07-15) — end-of-trip summary, `pct_time_in_zone` now populated |
| Surface Area Check | **In progress (2026-07-16)** — settings/privacy screen shell built; consent/onboarding screen still open (see below) |
| Retention Hook | **Next session** — has an ethical caveat, read before building (see below) |

- **Color-intensity comparison dev tool (same-day, second follow-up)** — user liked the "more splash" pass but wanted a way to dial it, and to name the two states so far: "color level 1" = the original single-accent pass, "color level 2" = the splash pass with `--accent-2`. Added a 0-5 dropdown (`#color-level-switch`) independent of the theme picker — theme picks *which* hue, level picks *how much* shows up. Level 0 strips all decorative color back to the literal pre-session flat black/white look (including neutralizing `--surface`/`--surface-border`, which are otherwise theme-tinted); level 1 reproduces the true first iteration (single accent, no `--accent-2` features at all); level 2 is today's default; levels 3-5 progressively tint previously-neutral secondary text (unit label, trip-progress lines, links), grow the background glows, and at 4-5 tint borders and turn the button fill into a two-tone gradient. Zone-indicator red/yellow/green untouched at every level, same invariant as the theme tool. Verified with Playwright across all 6 levels × the violet/gold theme (chosen to stress-test since its two accents are furthest apart in hue) plus a combined theme×font×level sweep, no console errors.

- **12 more palettes, a "mode" (texture/lighting) dev tool, and ~21 more fonts (same-day, third follow-up)** — user supplied 12 more palette-site screenshots to add (`crimson-gold`, `royal-blue`, `lime`, `sky-pastel`, `ultraviolet`, `candy`, `desert`, `citrus`, `ember`, `magenta-pastel`, `cyber-teal`, `tropical` — theme picker is now 17 options total), asked for a new independent "mode" axis (neon/pastel/light/metallic/glass/retro terminal), and asked for "a very wide range" of fonts. Several of the new palettes are inherently green/yellow/red-dominant with no cool-hued alternative in the source palette at all (`lime` especially — every swatch is a shade of green, the same hue family as the zone indicator's own state) — implemented faithfully per the user's explicit picks rather than substituted, with prominent code comments flagging which ones to judge live rather than trust from a screenshot; the zone indicator itself is untouched regardless. The mode axis (`#mode-switch`, `data-mode`) is orthogonal to theme (hue) and color-level (intensity) — it changes texture/lighting/structure: neon flattens the background and turns buttons into glow-bordered outlines; pastel and light both flip to a light background (pastel stays stylized/soft, light stays plain/functional, zone-state backgrounds get a legibility opacity bump on light backdrops — same hues, not reassigned); metallic adds a brushed-gradient button/card treatment via `color-mix()`; glass adds frosted `backdrop-filter` panels; retro is the one deliberate exception that forces its own fixed terminal-green palette and monospace font plus a scanline overlay, regardless of the theme/font pickers. Fonts went from 6 to 26 options spanning sans/serif/slab/monospace/display/script/novelty (Optima, Verdana, Trebuchet, Baskerville, Palatino, Times, Didot, American Typewriter, Courier New, Impact, Arial Black, Copperplate, Snell Roundhand, Zapfino, Bradley Hand, Marker Felt, Noteworthy, Chalkboard SE, Comic Sans MS, Papyrus, etc.) — all either CSS generic keywords or fonts Apple ships on-device, still zero network dependency. Verified: all 6 new modes + a sample of new themes/fonts via Playwright screenshots, a full sweep through every theme × font × mode × color-level combination, and a 4-axis combo (cyber-teal + neon + monospace + level 5) to confirm they compose without conflict — no console errors anywhere. One cosmetic quirk noted, not fixed: Papyrus's unusual glyph proportions clip slightly at the top of the big speed number — expected/acceptable for a font-comparison tool, not something to paper over.

## Done (2026-07-15, this session)

- **Drive-simulation profiles** — added Residential, Inner City, Highway, and Rural profiles alongside the original "Full range" one, selectable via a dropdown next to the simulate button (`SIMULATED_DRIVE_PROFILES` in `js/app.js`). Verified with Playwright: each produces speeds/zone states in its intended range with no console errors.
- **Trip baseline mismatch** — resolved by replacing the legacy "Trip: ±X vs 55mph" live readout (`setTripTimeSavedDisplay`, `TRIP_BASELINE_SPEED_MPH`) with a live "% of trip in zone so far" readout (`setTripZoneProgressDisplay`, `#trip-zone-progress`), the same running stat the end-of-trip summary shows. `TRIP_BASELINE_SPEED_MPH` is gone entirely, so the corresponding CLAUDE.md pre-launch checklist item is removed too.
- **Traffic-light zone indicator** — the live Core Function display is now a 3-state red/yellow/green readout instead of 2-state yellow/green, matching an intuitive "green = speed up, yellow = nearing the limit, red = won't help" mental model. `ZONE_THRESHOLD_SECONDS` (60s/~73mph, the red/not-red line) is unchanged and still what `pct_time_in_zone` is computed from; the new `ZONE_NEARING_THRESHOLD_SECONDS` (120s/~50mph) only splits the green/yellow sub-states for the live display — see CLAUDE.md's "Traffic-light zone indicator" note for the full rationale.
- **End-of-trip summary reworked (same day, follow-up)** — dogfooding a simulated highway drive showed the original "X% of this trip, more speed would have helped" reads as "you should've gone faster than 80mph" when a trip stays near the zone ceiling the whole time, backwards from the app's point. Replaced with a concrete "Xs behind the fastest pace that would have helped," computed only over the in-zone portion of the trip vs. the ideal time at the exact zone-ceiling speed (`secondsBehindPace`/`zoneCeilingMph()` in `js/app.js`). Verified: highway sim → 6s, residential sim → 45s. `pct_time_in_zone` unchanged in the database, only the on-screen number changed. Full math, including which thresholds are and aren't literature-backed, is now written out in `README.md`'s "How the pace/zone math works" section.
- **Live in-trip readout gets the same time number (same day, follow-up)** — added a second line under the live "% of trip in zone so far" showing a running version of the same seconds-behind-pace number the end-of-trip summary shows (`#trip-zone-progress-time`, computed in `recordSample`). Kept the percentage as the headline per explicit ask ("I like the percentage... maybe below it we could ALSO include the time calculation") rather than replacing it.
- **GPS fallback speed bug — partially fixed (same day, follow-up)** — reported as "huge numbers on refresh, takes a few tries" during dev-server testing. Root cause: desktop browsers have no GPS chip, so `coords.speed` is essentially always null and the app falls back to computing speed from the distance/time delta between fixes; desktop Wi-Fi/IP-based positioning is coarse (accuracy routinely hundreds+ meters) and jumps between fixes, so a large apparent jump over a short time delta produced a physically impossible speed with nothing catching it. Added two guards in `handlePosition` (`js/app.js`): a fix isn't remembered as "last known position" for the delta calc unless its reported accuracy is within `MAX_FIX_ACCURACY_METERS` (100m), and any resulting speed above `MAX_PLAUSIBLE_MPH` (200) is discarded. This stopped the triple-digit/absurd readings, verified with Playwright. **Not fully fixed** — see the open item below, found by the user immediately after this fix landed.

## Done (2026-07-21, this session)

- **Visual identity / declutter pass** — cosmetic-only, deliberately scoped to avoid touching data/logic/thresholds while professor feedback on the 2026-07-16 build is still pending. Added an accent color + surface-color token system (`css/style.css` `:root`), a subtle gradient background replacing flat black, `ui-rounded` numeric styling for the speed/pace/zone/trip-summary numbers (zero-dependency — system font keyword, not a font file), real inline-SVG icons replacing the raw `⚙` glyph and text wordmark, and card-style settings sections with icons instead of an unbroken text wall. Also dropped the redundant standalone "Recording…" line from the mid-trip readout (the trip button already says "End Trip") — did not touch the protected "Xs behind the ~73mph efficient pace" wording. Verified with Playwright across all screens/orientations plus a console-error check. Full writeup in CLAUDE.md's "Visual identity pass" note.
- **Palette + font comparison dev tools (same-day follow-up)** — user wanted to try 4 candidate color palettes (screenshotted from an external site) and several font choices live in the app before picking one. Added two dev-only `<select>`s (`#theme-switch`, `#font-switch`) in the existing "Dev tools ▸" disclosure, both persisted via `localStorage` so a choice survives navigation/reload. 4 themes (purple/magenta, navy/teal, violet/gold, sky/sunset — the user's stated favorite) each set `--accent`/`--accent-2`/background-glow/surface tokens; the zone indicator's red/yellow/green and the base near-black background level are never themed. For the sky/sunset favorite, pulled `--accent` from the palette's cool end (sky blue) rather than orange/red, which sit close to the zone indicator's own yellow/red hues and also read as a step back toward the warm "AI generated app" look from earlier the same session — the warm tones still show up as a secondary accent/background wash, just not on the zone indicator or primary buttons. Also expanded color usage overall per user feedback that the first pass felt "too minimal": added `--accent-2` (pace readout color, a settings icon chip, a second background glow, a secondary button-shadow tint) and a two-tone brand mark icon. 6 font options (default, rounded, serif, monospace, Avenir Next, Futura) swap `--font-body`/`--font-numeric` — all either CSS generic keywords or fonts Apple ships on-device, so still zero network dependency. Both tools are temporary; see CLAUDE.md's pre-launch checklist for what to fold in vs. delete once a final palette/font is chosen. Verified with Playwright across all theme/font combinations, both orientations, no console errors.

## Done (2026-07-16, this session)

- **"Behind the fastest pace" wording fixed in both spots** — user picked the "zone + reference speed" hybrid style out of 4 drafted candidates. Live in-trip line (`#trip-zone-progress-time`, `setTripZoneProgressDisplay` in `js/app.js`) now reads `"Xs behind the ~73mph efficient pace so far"`; end-of-trip caption (`tripSummaryCaptionEl`, `showTripSummary`) reads `"behind the ~73mph efficient pace"`. The `~73` is computed live as `Math.round(zoneCeilingMph())`, not hardcoded, so it stays correct if the underlying thresholds ever change. Syntax-checked; screenshot verification was explicitly skipped by the user for this text-only change (no layout/CSS touched).
- **Settings/privacy screen built (Surface Area Check, partial)** — new real 4th screen (`#settings-screen` in `index.html`, toggled via `settings-nav`/`settings-back` in `js/app.js`), reachable via a gear icon top-left of the live dashboard. Contains an "About Paceometer" section (plain-language pace formula, traffic-light meaning, end-of-trip number, explicit "doesn't know the speed limit" caveat) and a "Privacy" section (no raw GPS ever leaves the phone, what summary fields are saved, RLS restricts access to own rows) — both honest about literature-backed vs. project-design-choice per the same standard as README.md. Sign-out button moved here from its old floating position in `#app`. `auth.js`'s `showApp()`/`showAuth()` now also hide `#settings-screen` on auth-state transitions, so it can't stay visible across a sign-out. Trip summary stays an inline swap within `#app` (explicit user decision, not promoted to a separate screen — no functional reason to, per the plan counting "screens" as UX concepts not literal routes). Verified with Playwright screenshots (portrait, landscape, scrolled to bottom, back-navigation) since this is real new layout, not just text.
- **Gear icon fixed — was tiny and unclickable (reported by user)** — real bounding box was ~12.5×21px (a bare glyph, no padding), and sat flush at the viewport edge instead of the intended 16px margin because `env(safe-area-inset-top, 16px)` resolves to a *defined* `0` on non-notch screens, so the fallback never kicked in. Fixed to a real 44×44px tap target with `max(env(...), 16px)` positioning; also removed the now-dead `.sign-out` CSS rule left over from moving that button into settings. Verified: click now reliably opens settings in both orientations.
- **Zone hysteresis halved** — `ZONE_HYSTERESIS_SECONDS` 10 → 5 (`js/app.js`), a direct UX-feel tuning request ("the padding just feels like too much"). Pure design-choice constant, not literature-derived (already documented as such), so no lit-review consultation needed — just implemented and syntax-checked. README/CLAUDE.md updated to match.
- **Security audit (app-security-audit skill)** — full pass given this is about to go to outside eyes. Rating: STRONG. No secrets committed, RLS correctly scoped with `WITH CHECK`/`auth.uid()`, identity always server-derived via `getUser()`, no XSS sinks (`textContent` only, no `innerHTML` anywhere), privacy invariant (no raw GPS in any payload) holds in the actual code. One quick win (bump pinned `supabase-js` from 2.110.2 to 2.110.6) applied and verified (app loads clean, no console errors). Full report given inline, not saved to a file.
- **Full walkthrough test (Playwright, portrait + landscape)** — drove the entire flow (auth → dashboard → start trip → simulated drive → end trip → summary → continue → settings → back → sign out) end to end. No console errors; the one page error (`Cannot read properties of null (reading 'id')`) is an expected artifact of bypassing real auth for the test (fake session has no real Supabase user), not a real bug. Confirmed the new wording renders correctly with live-computed numbers (e.g. "100% of trip in zone so far" / "4s behind the ~73mph efficient pace so far").
- **Dev-tool visual decluttering** — the driving screen was legitimately busy: live readout + zone box + trip controls + progress lines, then loud orange-dashed simulate controls sitting directly below, all visible at once. Collapsed the simulated-drive dropdown/button/progress bar behind a small "Dev tools ▸" disclosure toggle (`#simulate-toggle` in `index.html`, listener in `js/app.js`'s DEV TOOL block), collapsed by default. Full functionality unchanged, one tap away; default view is now just the real product surface. Verified in both orientations. Pre-launch checklist entry in CLAUDE.md updated to include removing the toggle too.
- **Docs consistency pass** — read README.md/CLAUDE.md/TODO.md fresh for a first-time reader (ahead of sending this to the professor). Found and fixed real staleness: CLAUDE.md's "Screen-gating pattern" still described only 2 screens (now 3, plus the settings toggle is a separate mechanism from the auth listener — documented explicitly so a future change doesn't conflate the two); the "End-of-trip summary" paragraph still described the wording and trip-summary-screen questions as open when both were resolved this session; no settings-screen documentation existed anywhere in CLAUDE.md despite it being a full architecture addition. README's feature list and file-structure diagram didn't mention the settings screen at all, and the dev-tool bullet didn't mention it's now collapsed by default. All fixed.

## Next session: remaining pipeline stages

- **UI decisions are still pending — pick and fold in the final theme/font/mode/color-level** — everything from today's three visual-pass sessions (18 themes, 26 fonts, 6 modes, 6 color-intensity levels) is exploratory dev tooling, not a finished design. Nothing is committed. Next step is the user actually picking one option from each of the four dropdowns (or a couple of specific combinations to compare with the professor) — then those choices get folded into `css/style.css`'s plain `:root` block and every other `data-theme`/`data-font`/`data-color-level`/`data-mode` block, plus all four switcher UIs (`#theme-controls`, `#font-controls`, `#color-level-controls`, `#mode-controls`) and their `localStorage` keys, get deleted rather than shipped. See CLAUDE.md's pre-launch checklist for the exact list of what to remove.
- **Accessibility pass needed, not yet started** — nothing in the app has been audited against WCAG/screen-reader/reduced-motion needs so far. Known candidates once picked up: color contrast (some of today's paler accents-on-dark and light-mode combinations haven't been checked against WCAG AA ratios; the zone indicator's low-alpha green/yellow/red backgrounds were tuned for legibility by eye, not measured); screen-reader support (icon-only buttons like `#settings-nav` and `#trip-summary-dismiss`'s dev-tool siblings need `aria-label`s audited, the `.zone-indicator`'s state changes aren't announced to assistive tech, e.g. no `aria-live`); `prefers-reduced-motion` isn't respected anywhere (the zone-flash pulse animation and retro mode's scanline effect both animate unconditionally); keyboard navigation/focus-visible styling hasn't been checked outside the auth form; and touch target sizing has only been explicitly audited for the settings gear (44×44px) so far, not every tap target in the app.

- **GPS fallback still shows spurious low speed while genuinely stationary (open, found 2026-07-15, right after the fix above landed)** — the `MAX_FIX_ACCURACY_METERS`/`MAX_PLAUSIBLE_MPH` guards stop the extreme/implausible readings (triple-digit+ mph), but the user reports still seeing "a few mph" or "double-digit" speeds on the dev server while the laptop is sitting still. Root cause is a different flavor of the same underlying problem: two consecutive fixes can differ by tens of meters just from ordinary position noise even when both fixes individually pass the 100m accuracy gate — desktop Wi-Fi positioning jitters within its own uncertainty radius even at rest, and the fallback formula (`distance / elapsedSeconds`) treats *any* apparent displacement as real motion, with nothing currently distinguishing "actually moved" from "GPS noise within the margin of error." Standard fix direction for later (not implemented — user asked to log this, not fix it now): only trust a delta as real movement if the apparent displacement exceeds the *combined* accuracy radius of the two fixes (`haversineMeters(a, b) > a.accuracy + b.accuracy`), treating smaller apparent movement as effectively stationary (mph = 0) rather than computing a speed from it. Likely much less visible on a real phone — GPS accuracy is roughly 5-10m vs. desktop Wi-Fi positioning's hundreds of meters, so the same at-rest jitter translates to a much smaller spurious speed, and a lot of it would likely already fall under `PACE_MIN_SPEED_MPH` (5) and simply not show a pace/zone reading — but per the user, "worse on desktop, not zero risk on the phone," so still worth the combined-accuracy fix eventually rather than assuming it's a dev-only artifact.
- **Surface Area Check: consent/onboarding screen still open** — the 4th of the plan's ~4 screens (consent/onboarding, live dashboard, trip summary, settings/privacy — `research_plan.qmd` `@sec-mvp-funnel`). Unlike settings/privacy, this one isn't safe to draft solo: `research_plan.qmd` `@sec-privacy` calls for "explicit, plain-language consent, not a legal boilerplate paragraph," tied to actual IRB approval status, so the copy needs sign-off from Professor Helveston's side rather than being invented here. Decide with the user how to sequence this (placeholder shell now vs. wait for approved language).
- **Retention Hook — design settled, build blocked on professor input (discussed 2026-07-16)** — talked through the plan's own ethical callout (research_plan.qmd `@sec-privacy`/`@sec-question`: a behavioral-enough engagement mechanic needs IRB consent-language disclosure, not just a neutral UX choice). Decisions made: (1) delivery style will be a **passive readout only** — "N days driven with Paceometer this week," shown in the live dashboard or settings screen, no notifications/banners/streaks/nudges, only visible if the driver looks — the lowest-risk option of the two discussed. (2) **Explicitly held off on writing any code** — user chose to wait until the IRB-disclosure question has an actual answer from Professor Helveston before building even this passive version, rather than building now and flagging it for later review. Next step is that conversation, not implementation.
